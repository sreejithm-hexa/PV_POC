/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn more about Gradle by exploring our samples at https://docs.gradle.org/7.4/samples
 */
buildscript {
    repositories {
        maven {
            url "https://repo1.maven.org/maven2"
        }
         //mavenCentral()
    }
    dependencies {
        classpath 'mysql:mysql-connector-java:8.0.28'
        classpath "org.flywaydb:flyway-gradle-plugin:5.2.4"
        //classpath 'org.flywaydb:flyway-core:8.5.0'
    }
}

apply plugin: 'org.flywaydb.flyway'

group = 'database'
version = '0.0.1-SNAPSHOT'

flyway {
    url = 'jdbc:mysql://3.22.94.249:3306/db'
    user = "sreejith" //"${username}"
    password = "@Admin123" //"${password}"
    //schemas = ['schema1', 'schema2', 'schema3']
}
